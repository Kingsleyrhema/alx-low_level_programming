#include <stdio.h>

/**
 * mian - main block
 *
 * Return: always return 0
 */

int main(void)

{
	int i;
	int I;
	for (i = 97; i < 122; i++)

		putchar(i);
	for (I = 65; i < 91; i++)

		putchar(I);
	putchar('\n');
	return (0);
}
